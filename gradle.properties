# Skip all npm work: ./gradlew build -Pfrontend.skip=true
# The :web jar is still produced (empty), so :api's dependency on it resolves
# either way. Use it for backend-only iteration; CI leaves it false.
frontend.skip=false

org.gradle.parallel=true
org.gradle.caching=true
