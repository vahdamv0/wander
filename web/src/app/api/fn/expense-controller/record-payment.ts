/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ExpenseView } from '../../models/expense-view';
import { PaymentRequest } from '../../models/payment-request';

export interface RecordPayment$Params {
  tripId: number;
      body: PaymentRequest
}

export function recordPayment(http: HttpClient, rootUrl: string, params: RecordPayment$Params, context?: HttpContext): Observable<StrictHttpResponse<ExpenseView>> {
  const rb = new RequestBuilder(rootUrl, recordPayment.PATH, 'post');
  if (params) {
    rb.path('tripId', params.tripId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ExpenseView>;
    })
  );
}

recordPayment.PATH = '/api/trips/{tripId}/expenses/payments';
