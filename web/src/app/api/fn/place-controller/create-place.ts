/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreatePlaceRequest } from '../../models/create-place-request';
import { PlaceView } from '../../models/place-view';

export interface CreatePlace$Params {
  tripId: number;
      body: CreatePlaceRequest
}

export function createPlace(http: HttpClient, rootUrl: string, params: CreatePlace$Params, context?: HttpContext): Observable<StrictHttpResponse<PlaceView>> {
  const rb = new RequestBuilder(rootUrl, createPlace.PATH, 'post');
  if (params) {
    rb.path('tripId', params.tripId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PlaceView>;
    })
  );
}

createPlace.PATH = '/api/trips/{tripId}/places';
