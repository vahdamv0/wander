/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PlaceView } from '../../models/place-view';
import { UpdatePlaceRequest } from '../../models/update-place-request';

export interface UpdatePlace$Params {
  tripId: number;
  placeId: number;
      body: UpdatePlaceRequest
}

export function updatePlace(http: HttpClient, rootUrl: string, params: UpdatePlace$Params, context?: HttpContext): Observable<StrictHttpResponse<PlaceView>> {
  const rb = new RequestBuilder(rootUrl, updatePlace.PATH, 'put');
  if (params) {
    rb.path('tripId', params.tripId, {});
    rb.path('placeId', params.placeId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PlaceView>;
    })
  );
}

updatePlace.PATH = '/api/trips/{tripId}/places/{placeId}';
