/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TripItinerary } from '../../models/trip-itinerary';

export interface GetItinerary$Params {
  tripId: number;
}

export function getItinerary(http: HttpClient, rootUrl: string, params: GetItinerary$Params, context?: HttpContext): Observable<StrictHttpResponse<TripItinerary>> {
  const rb = new RequestBuilder(rootUrl, getItinerary.PATH, 'get');
  if (params) {
    rb.path('tripId', params.tripId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TripItinerary>;
    })
  );
}

getItinerary.PATH = '/api/trips/{tripId}/itinerary';
