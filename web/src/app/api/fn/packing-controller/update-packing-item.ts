/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PackingItemRequest } from '../../models/packing-item-request';
import { PackingItemView } from '../../models/packing-item-view';

export interface UpdatePackingItem$Params {
  tripId: number;
  itemId: number;
      body: PackingItemRequest
}

export function updatePackingItem(http: HttpClient, rootUrl: string, params: UpdatePackingItem$Params, context?: HttpContext): Observable<StrictHttpResponse<PackingItemView>> {
  const rb = new RequestBuilder(rootUrl, updatePackingItem.PATH, 'put');
  if (params) {
    rb.path('tripId', params.tripId, {});
    rb.path('itemId', params.itemId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PackingItemView>;
    })
  );
}

updatePackingItem.PATH = '/api/trips/{tripId}/packing/{itemId}';
