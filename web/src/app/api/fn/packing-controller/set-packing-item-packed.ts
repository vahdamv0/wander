/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PackedRequest } from '../../models/packed-request';
import { PackingItemView } from '../../models/packing-item-view';

export interface SetPackingItemPacked$Params {
  tripId: number;
  itemId: number;
      body: PackedRequest
}

export function setPackingItemPacked(http: HttpClient, rootUrl: string, params: SetPackingItemPacked$Params, context?: HttpContext): Observable<StrictHttpResponse<PackingItemView>> {
  const rb = new RequestBuilder(rootUrl, setPackingItemPacked.PATH, 'put');
  if (params) {
    rb.path('tripId', params.tripId, {});
    rb.path('itemId', params.itemId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PackingItemView>;
    })
  );
}

setPackingItemPacked.PATH = '/api/trips/{tripId}/packing/{itemId}/packed';
